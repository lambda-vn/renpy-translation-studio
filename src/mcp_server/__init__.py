"""MCP server exposing one Ren'Py project to a translating client.

Lets an assistant that already holds a subscription do the translating,
where the application's own providers need an API key or a local GPU.
The tools are the actions of the review screen, so what comes back is
reviewed there like any other suggestion.
"""
